[package]
publish = false
name = "example-no-ndarray"
version = "0.0.0"
edition = "2024"

[dependencies]
executorch = { path = "../../executorch/", default-features = false, features = [
    "module",
    "data-loader",
] }
